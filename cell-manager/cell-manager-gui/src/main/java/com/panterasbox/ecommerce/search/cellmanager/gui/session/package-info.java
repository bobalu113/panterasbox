/**
 * This package contains a couple Spring override beans because
 * some of the session code in foundation is incompatible with
 * the H2 database.  Once we create the real session tables in
 * Oracle, we can get rid of this entire package.
 */
package com.panterasbox.ecommerce.search.cellmanager.gui.session;

